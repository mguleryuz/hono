- never commit without asking for permission
- always use src/routes ,src/api, src/services, src/schemas to add new routes and their corresponding services and schemas
- always use and if not present create a new hook in client/src/hooks to handle the api flows
- always use tanstack-effect to handle the api flows